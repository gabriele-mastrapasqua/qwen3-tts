Task: P2.1 P2.2 P2.3 P2.4 P2.5 P2.6 (runtime parity) — evidence base for the whole P2 block
Question: which of the 2026-09-04 runtime changes reach which backend, through which gate, and what is left?
Written: 2026-09-05 from the static audit (public summary: docs/cross-backend-audit-2026-09-05.md §1). Line numbers = tree at 620279c..4e8d447.

Classification (A common runtime · B common design, backend impl · C x86 VNNI only)

| change (commit) | class | gate / mechanism | reaches |
|---|---|---|---|
| decoder tiles on the engine pool, `QWEN_SD_POOL=qwen` (a449f60) | A | `sd_pool_run` (kernels.c ~8534): `qwen_sd_pool_mode()` → `qwen_parallel(nt, sd_gcd_task)`; inline `fn(ctx)` when `qwen_parallel_active()`; default set ONLY in `qwen_tts_serve_continuous` (qwen_tts.c ~2667: `qwen_sd_pool_default(1); qwen_blas_own(1);`) | batched server (`--batch-size>=2`, incl. prefork+batch) on every CPU backend/OS. NOT `qwen_tts_serve_ex` (server.c ~1906: plain `--serve`, prefork without batch) and NOT the CLI → those keep the private `sd-pool` team (`sdp_worker_main`, `SD_POOL_MAX_WORKERS`) |
| OpenBLAS at 1 thread + partitioned decoder SGEMM (a449f60, `qwen_tts_sd_gemm.c`) | B | `qwen_blas_set_threads` (kernels.c ~94): weak `openblas_set_num_threads` under `#if __GNUC__ && !__APPLE__`, else no-op; `qwen_blas_own_get()` reads `QWEN_BLAS_OWN`; `sdg_run` partitions when own && nt>1 && RowMajor && !`qwen_parallel_active()` && M·N·K >= 2^18, slices 16-aligned; speech_decoder.c:31 `#define cblas_sgemm qwen_sd_sgemm`; CUDA `SD_GEMM` (speech_decoder.c ~85) tries `qwen_cuda_sd_sgemm` first, CPU fallback partitioned | Linux+OpenBLAS any ISA. **macOS**: BLAS cap is a no-op but the partition still turns on → Accelerate slices on the GCD pool with Accelerate's own threads (unmeasured). Windows: no weak symbol, unchanged |
| `qwen_parallel_active()` nested-dispatch guard | B | thread.c pthread branch: TLS `g_qp_depth` (~318, inc/dec in `run_chunks` ~333/342); GCD (~86) and Windows (~212) stubs return 0 | pthread only. GCD safe (`dispatch_apply` reentrant); Windows unreachable (only in-region callers are the VNNI regions) |
| LOW pool priority for the prefill helper, `QWEN_PREFILL_LOW_MS` / `QWEN_POOL_HI_WINDOW_US` (d528dc5) | B | `qwen_parallel_set_low_until` (thread.c ~323), `g_qp_last_hi_ms` wait before `submit_mtx` (~492-498); helper sets it in qwen_tts.c ~2250; GCD/Windows stubs no-op | pthread only; opt-in (default 0) |
| CP step as one region, `QWEN_CP_REGION` (b152946) | **C** | `cp_region_ok` (code_predictor.c ~1111): `qwen_parallel_team()>=2` && all 6 layer weights int8 && none q4 && `qwen_i8mm_qkv_usable`/`qwen_i8mm_usable` at B=2 (static, once) and at BW per step; `cp_region_task` 8 barriers/layer, `qwen_barrier_*` (thread.c ~16); `qwen_i8mm_*` compiled only `#if __AVX512VNNI__` (kernels.c ~10277-10330) | x86 AVX-512 VNNI, int8, pthread, BW 2..16, not when AMX would take the shape (see .work/p1-4-amx-runtime.md). Off: Arm (SMMLA/KleidiAI), AVX2, AVX-512F, int4/bf16, GCD (`qwen_parallel_team()==1`) |
| Talker step as one region, `QWEN_TK_REGION` (faa496c) | **C** | `tk_region_run` (talker.c ~2175), called at ~2234 in `batch_talker_step_impl`; same gate | same as CP region; 112 dispatches/step elsewhere |
| batched CP heads, `QWEN_CP_BATCH_HEAD` (110ff48) | C gate / A body | `cp_batch_mtp` / `cp_batch_lm` (~1193/1211): `cp_mtp_proj_int8` / `cp_lm_head_int8[g]` present, not q4, `BW 2..16`, `qwen_i8mm_usable`; body = `qwen_matmat_int8` (generic dispatcher) + per-column argmax | VNNI int8 only. Port = gate change; acceptance must be **code (argmax) parity**, not bits: KleidiAI i8 quantises the LHS per row (`kai_i8_pack_lhs`), the VNNI path shares `quantize_act_int8_col` with the GEMV |
| streaming decoder arena + stream recycling (b0570a6) | A | speech_decoder.c: `sd_arena_t` (32 MB blocks, ≤16, spills counted), `sd_tmp_alloc/calloc/malloc/free`, `g_sd_arena` TLS set in `qwen_speech_decoder_decode_streaming_st`, `g_sd_retired[8]` recycling in `qwen_sd_stream_init/free`; `qwen_sd_stream_state_t.scratch` (qwen_tts.h) | every CPU backend/OS; CUDA/Metal servers whenever the conv decoder runs on CPU (`g_cuda_decoder_conv_on` off; metal.m has no decoder). Non-streaming `conv_decoder_forward` (~1557) still `aligned_calloc` per call (CLI without `--stream`) |
| grow-once TLS scratch in the int8 conv workers (b0570a6) | B | `QWEN_MM_SCRATCH(sdcolf/sdcolq/sdsa)` (kernels.c ~3113-3133) in `sd_conv1d_worker` (~8787) and the scalar path of `qwen_conv1d_int8`; tiles `sd_tile_2x4/1xN` compiled `#if __ARM_FEATURE_DOTPROD || __AVX512VNNI__` (~8577-8752); `qwen_sd_int8_available` (~8404) | VNNI (default on) and Arm DOTPROD (opt-in `QWEN_SD_INT8=1`). AVX2/AVX-512F: no int8 conv at all (fp32 im2col + SGEMM, buffers in the arena) |
| per-thread PCM buffer in `send_pcm_chunk` (server.c) | A | TLS grow-once | every backend |
| thread names (63af9dc) | A | `prctl(PR_SET_NAME)` Linux / `pthread_setname_np` Apple: thread.c ~349, kernels.c `qwen_thread_name_k` ~8479, server.c ~1717, qwen_tts.c ~2224; includes guarded `#if __linux__` | Linux, macOS; Windows unnamed |
| uncommitted A1/A2a, `QWEN_PREFILL_INT8MM`, `QWEN_PREFILL_CHUNK` (working tree only) | C | AVX-512 BF16/VNNI, opt-in, off | nothing by default |

GPU precedence: `batch_cp_transformer_step` checks `g_cuda_cp_batch_state` (B<=16) then `g_metal_cp_batch_state` (B<=8) BEFORE `cp_region_ok`; `qwen_batch_talker_step_ragged` checks `g_cuda_talker_batch_state` / `g_metal_talker_batch_state` before `batch_talker_step_impl`. So the CPU regions never run under a fused GPU backend; the CPU decoder + arena + execution budget do.

Pool backends (P2.6): pthread = real (`qwen_parallel_active`, `qwen_parallel_set_low_until`, `qwen_parallel_team`, `qwen_barrier_*`, `QWEN_POOL_SPIN` 4096, `QWEN_POOL_NARROW`, `qwen_parallel_is_reentrant` = `QWEN_PREFILL_HELPER`); GCD (`__APPLE__ && __BLOCKS__ && !QWEN_FORCE_PTHREAD`, thread.c ~61-88) = `dispatch_apply`, `is_reentrant()=1`, `active()=0`, `team()=1`, `set_low_until` no-op, `qwen_pool_spin_value()=-1`; Windows (~90-214) = same stubs, own pool. Intended contract not written anywhere yet.

Remaining per-call allocations (P2.4): kernels.c ~3158 `qwen_matmat_bf16` AMX branch `Xb`; ~3204 BFMMLA branch `Xb` (Arm Linux with `QWEN_NO_KAI_BF16`); ~5019 `qwen_matmat_bf16_qkv` `Xb` (AVX-512 BF16 and AMX batched bf16 QKV); ~5509-5521 `qwen_matmat_q4_0` DOTPROD-only `B x matvec` fallback `xcol/ycol` (Apple M1 int4 at B>=2: no i8mm → no KleidiAI, no SMMLA); q8repack.c ~364 `tmp` at B>1; kernels.c ~9032-9046 Apple snake vDSP `temp` per channel row. VNNI/AVX-512BF16/AMX-int8 use `mm_scratch_*`; KleidiAI uses `kai_scratch_*` (TLS); none per call.

M1 compile check (2026-09-05, `make blas`, clang, GCD): clean; warnings only: unused `kt_t0`/`kt_B` in the non-x86 branches of `qwen_matmat_int8_qkv` (~4870) and `qwen_matmat_bf16_qkv` (~5009), unused `batch_proj` on Arm (talker.c ~1827), pointer-bool `ctx->layers` (server.c ~1765). `--caps` on M1: "NEON dotprod/SDOT, M1-class (Armv8.5, dotprod only)".

Files/functions inspected: qwen_tts_thread.c (all three branches), qwen_tts_kernels.c (`sd_pool_run`, `qwen_blas_*`, `qwen_i8mm_*`, `QWEN_MM_SCRATCH`, conv tiles, snake, dispatchers), qwen_tts_sd_gemm.c, qwen_tts_speech_decoder.c (arena, BLAS define, `conv_decoder_forward*`, `sd_int8_enabled`), qwen_tts_code_predictor.c (`cp_region_ok`, `cp_batch_*`, GPU checks), qwen_tts_talker.c (`tk_region_run`, `qwen_batch_talker_step_ragged`, `qwen_batch_proj_q/qkv`), qwen_tts.c (serve init ~2660-2672, helper ~2250), qwen_tts_server.c (serve_ex/batched/prefork entry points), qwen_tts_backend.c, qwen_tts_kleidi.c, qwen_tts_q8repack.c, qwen_tts_metal.m (decoder absent), Makefile.

Implementation gaps, in order: (1) P2.5 execution budget in `qwen_tts_serve_ex` (one call pair) + macOS: gate `sdg_run` on `qwen_blas_threads_now()==1` or cap Accelerate via `VECLIB_MAXIMUM_THREADS`; (2) P2.1/P2.2 region runners for AMX tiles and KleidiAI i8 GEMM row blocks (region bodies are ISA-neutral; only `qwen_i8mm_run*` are VNNI); (3) P2.3 batched heads gate widened, argmax-parity acceptance; (4) P2.4 the six allocations above; (5) P2.6 write the pool contract (which API is real where) before any GCD work.
